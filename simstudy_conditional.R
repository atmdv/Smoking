
# Add conditions to data
#defCondition <- defCondition(condition="sex==1", formula="0.079;0.146;0.146;0.180;0.169;0.152;0.128", dist="categorical")
#dt <- addCondition(defCondition, data, "age")

# Test simlated data
#prop.table(table(data$sex))
